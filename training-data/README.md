# Reviewed training inputs

Place approved, licensed real observations and their reviewed manifest here.
No real dataset has been supplied. Do not train a safety model on the synthetic
showcase readings or rename them as verified data. See docs/open-source-stack.md
and `python -m app.risk.training --help` for the validation and training contract.
