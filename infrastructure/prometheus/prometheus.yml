global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: saferoute-api
    metrics_path: /metrics/
    bearer_token_file: /run/secrets/metrics_token
    static_configs:
      - targets: ["api:8000"]
